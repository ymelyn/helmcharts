---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fixicc-agent-configs-pvc
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - {{ .Values.storage.accessModes }}
  storageClassName: {{ .Values.storage.class }}
  resources:
    requests:
      storage: {{ .Values.storage.fixicc_agent_configs.size }}
